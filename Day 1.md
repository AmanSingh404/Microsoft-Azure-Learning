# Day 1 – Virtual Machine Deployment

## Objective
Deploy a Linux VM in Azure and host a basic web server.

## Architecture
Azure Region → Resource Group → VNet → VM → Public IP → Internet

## Commands Used
sudo apt update
sudo apt install nginx -y

## Learning
Understood virtualization and public IP access.
